# SH1 — Разведка (ничего не менять)

> Предварительно прочитать `SH0_README_share.md`.
> **На этом этапе запрещено менять любой файл.** Только чтение и письменный отчёт.

## Цель

Установить фактические имена и механизмы. Дальнейшие этапы ссылаются на то, что должно быть выяснено здесь.

## Что прочитать

- `src/Clayzor.Lib.Web.Controls/AGENTS.md`, `docs/clay-grid.md`
- `Components/Grid/promts/_done/_readme_grid_dynamic.md` (план G0–G14), `GF0`, `GG0`, `GN0`, `GE0`, `GB0`
- `src/Clayzor.Lib.DALC/AGENTS.md` и `DbManager`
- `src/Clayzor.Lib.Entities/DynamicGrid/` — `ClayGridDefinitionData`, `DynamicSql`, `ClayGridSchemaMap`
- `Components/Grid/ClayGrid*.razor`, `ClayGrid.Dynamic.cs`, `ClayGridPageBase`
- `ClayColumnSettingsDialog`, `ConfirmDialog`, `ClayButton`, `ClayMenu`
- `src/Clayzor.Lib.Web.Settings/ClayAppSettings.cs`, `appsettings.json`, `web.config`
- `Components/Grid/Dynamic/ClayGridDynamicSettings.cs` и `Components/Grid/Dynamic/ServiceCollectionExtensions.cs` — настройки динамического режима и их регистрация
- `Components/Grid/ClayGridOptions.cs`; раздел «Настройки компонентов (Options)» в `src/Clayzor.Lib.Web.Controls/AGENTS.md`
- `scripts/dynamic-grid/schema.sql`

## Вопросы, требующие письменного ответа с указанием файла и строки

**А. Пользовательские параметры**
1. Полный перечень того, что сохраняется в `ClayGridUserParams`: для каждого — фактическое значение `Параметр` целиком и формат `Значение`.
2. Префиксы имён параметров: подтверди, что это **свойства** `ClayGridDynamicSettings` (`ColumnsParamPrefix`, `FilterParamPrefix`, `GroupingParamPrefix`, `SortingParamPrefix`, `PageSizeParamPrefix`, префикс быстрого поиска), приведи фактический список и значения по умолчанию. Входит ли в ключ идентификатор грида и как именно он приклеивается? Есть ли где-то константы-дубликаты этих значений?
3. Через какие методы/класс идёт **чтение** и **сохранение** параметров? Приведи подписи. Есть ли **единая** точка сохранения или пишут несколько разных мест?
4. Чем определяется `КодНастройкиКлиента` — откуда берётся идентификатор текущего пользователя?
5. В какой момент параметры сохраняются: сразу при изменении группировки/фильтра/сортировки или только по «Применить» в диалоге колонок?

**Б. Транзакции и DALC**
6. Умеет ли `DbManager` работать в транзакции (`BeginTransaction`, передача `IDbTransaction` в Dapper)? Если нет — как в проекте обеспечивается атомарность нескольких запросов?
7. Как `DbManager` реагирует на `SqlException` и что при этом видит пользователь (`ISqlErrorHandler` → `ClayErrorBar`)? Можно ли подавить показ баннера для ожидаемой ошибки, и есть ли для этого штатный способ?

**В. URL и маршрутизация**
8. Какой фактический URL страницы динамического грида? Приведи `@page`-директиву. Идентификатор грида — часть маршрута или query-параметр? Как он называется?
9. Какие ещё query-параметры страница читает сейчас (фильтры через `КлючURL`, номер страницы, сортировка)? Есть ли готовый хелпер разбора/сборки query-строки?
10. Как в проекте уже открываются ссылки в новом окне (после задачи с кнопкой документации) — есть ли готовый механизм `Href`/`Target` или JS-хелпер? Приведи его.

**Г. Инфраструктура UI**
11. Как устроена панель инструментов грида: порядок кнопок, компонент (`ClayButton`?), CSS-классы (`toolbar-*`), где заканчивается панель. Отдельно — **в каком файле физически объявлены стили `toolbar-*`**: `wwwroot/css/clay.css` библиотеки или `wwwroot/css/app.css` приложения. От этого зависит, куда класть класс новой кнопки (`SH5`).
12. Как показываются временные всплывающие сообщения (Snackbar) — через какой сервис, с какими настройками, есть ли обёртка?
13. Как вызывается `ConfirmDialog` — подпись, параметры, что возвращает?
14. Какие JS-файлы RCL уже подключены в `App.razor` и как регистрируются новые (`_content/Clayzor.Lib.Web.Controls/js/...`)?
15. Есть ли готовый компонент/паттерн для раскрывающегося списка с элементами и кнопками (`ClayMenu`?), и как он закрывается программно?

**Д. Конфигурация**
16. Подтверди по коду состав `ClayGridDynamicSettings`: ключи секции `"ClayGrid:Dynamic"`, свойства с именами объектов БД (`SettingsTable`, `ColumnsTable`, `UserParamsTable`), префиксы имён параметров, `GridIdQueryParam`/`ClientIdQueryParam`, содержимое `Validate()`, способ регистрации (`AddClayGridDynamic`). Новые параметры задачи обязаны следовать той же схеме. **Не путать с `ClayAppSettings`** (`Clayzor.Lib.Web.Settings`): это настройки приложения вообще, к именам объектов динамического грида отношения не имеют.
17. `ClayGridOptions` (`Components/Grid/ClayGridOptions.cs`): состав свойств, как компонент читает настройки внутри (поле `_opt`, `ResolveOptions`), чем определяется динамический режим и где это проверяется. Нужно для `SH5` и `SH7`: кнопки добавляются только в динамическом режиме, а параметра тега `Dynamic` больше нет.

## → verify

Отчёт на 20–30 строк: по пункту на вопрос, с файлом и строкой. Отдельно в конце:

- **Есть ли единая точка сохранения параметров** (ответ на вопрос 3) — от этого напрямую зависит выполнимость `SH8`. Если точек несколько, перечислить все.
- **Полный список имён параметров текущего грида** — основа `SH4`.
- **Фактический вид URL грида** с примером — основа `SH6`.
- Список вопросов, на которые ответа в коде нет: задать их через question tool, **не придумывая**.
