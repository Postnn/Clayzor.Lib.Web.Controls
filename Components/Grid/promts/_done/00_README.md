# Настраиваемый фильтр ClayGrid — мастер-план (порядок выполнения)

Цель: рекурсивный конструктор фильтров (query-builder в стиле Telerik Filter)
поверх существующего грида, с переиспользованием его типов (`ColumnType`,
`ColumnFilterOperator`, `LogicalOperator`, `ColumnFilter`, диалоги через `IDialogService`).

## Статус
- **01 — расширение типов (Date/Decimal/NULL) — СДЕЛАНО, закоммичено.**
  (Его switch-подход к типам далее рефакторится в задаче 03.)
- **02 — рекурсивные типы фильтра — СДЕЛАНО, закоммичено.**
- **03 — типобезопасная модель колонок (дескрипторы) — СДЕЛАНО, закоммичено.**
- **04 — вынос @code в ClayGrid.razor.cs — СДЕЛАНО, закоммичено.**
- **05 — разбиение code-behind по темам — СДЕЛАНО, закоммичено.**
- **06 — разбиение `ClayGridPageBase` на partial — СДЕЛАНО, закоммичено.**
- **07 — SQL по дереву фильтра — СДЕЛАНО, закоммичено.**
- **08 — Общий редактор значения (`ClayFilterValueEditor`) — СДЕЛАНО, закоммичено.**
- **09 — Рекурсивный UI + диалог фильтра (`ClayFilterDialog`) — СДЕЛАНО, закоммичено.**
- **10 — Интеграция в грид: данные/загрузка (CompositeFilter) — СДЕЛАНО, закоммичено.**
- **11 — Интеграция в грид: панель и маршрутизация — СДЕЛАНО, закоммичено.**
- **F1 — Исправления диалога настраиваемого фильтра — СДЕЛАНО, закоммичено.**
- **F2 — Панель грида: текст составного фильтра ИЛИ чипы колонок — СДЕЛАНО, закоммичено.**
- **F3 — Компоновка и компактность диалога — СДЕЛАНО, закоммичено.**
- **F4 — Диалог: реальное ограничение высоты + переключатель И/ИЛИ — СДЕЛАНО, закоммичено.**
- **F5 — Диалог: фиксированная высота + 3 зоны + плотность — СДЕЛАНО, закоммичено.**
- **F6 — Один скролл, текст вместо чипов, фокус на значение — СДЕЛАНО, закоммичено.**
- **F7 — Перетаскивание колонки при активном составном фильтре — СДЕЛАНО, закоммичено.**
- **F8 — Убрать дублирование текста фильтра и двойной скролл — СДЕЛАНО, закоммичено.**
- **F9 — Диалог: перетаскивание + фикс. высота + один скролл + удаление колоночных условий — СДЕЛАНО, закоммичено.**
- **F10 — Скролл у выпадающего списка оператора — СДЕЛАНО, закоммичено.**
- **F11 — Колоночное условие с двумя клаузами: показывать оба в форме фильтра — СДЕЛАНО, закоммичено.**
- **14 — Выбор колонок перед печатью/экспортом — СДЕЛАНО, закоммичено.**
- **F12 — Кнопки «добавить условие/группу» — в строку переключателя И/ИЛИ — СДЕЛАНО, закоммичено.**
- **G1 — Группировка: единая «хост-колонка» для строки-заголовка — СДЕЛАНО, закоммичено.**
- **G2 — Группировка: строка-заголовок на всю ширину (CSS) — СДЕЛАНО, закоммичено.**
- **G3 — Фикс сдвига колонок при смене группировки — СДЕЛАНО, закоммичено.**
- **G4 — Фикс наложения абсолютного блока на строки данных — СДЕЛАНО, закоммичено.**
- **G5 — Фикс схлопывания высоты строки-заголовка группы — СДЕЛАНО, закоммичено.**

## Порядок выполнения (одна задача за подход)
| № | Задача | Тип | Зависит от |
|---|--------|-----|------------|
| 03 | Типобезопасная модель колонок (дескрипторы) | рефакторинг | 01 |
| 04 | Вынос `@code` грида в `ClayGrid.razor.cs` | рефакторинг (механика) | — |
| 05 | Разбиение code-behind грида по темам | рефакторинг | 04 |
| 06 | Разбиение `ClayGridPageBase` на partial | рефакторинг | — |
| 07 | SQL по дереву фильтра | фича | 02, 03 |
| 08 | Общий редактор значения (`ClayFilterValueEditor`) | фича | 03 |
| 09 | Рекурсивный UI + диалог фильтра | фича | 02, 08 |
| 10 | Интеграция в грид: данные/загрузка | фича | 07, 06 |
| 11 | Интеграция в грид: панель и маршрутизация | фича | 10, 09, 05 |
| 12 | Сериализация / URL / индикатор / локализация | опц. | 02, 11 |
| 13 | Автотесты | качество | 07, 10 |
| 14 | Выбор колонок перед печатью/экспортом | фича | 11 |

Логика порядка: сперва **рефакторинги** (03–06) на чистую, проверяемую базу,
затем **новые компоненты** (07–09), затем **сборка всего вместе** (10–11),
после — опции и тесты.

## Правила работы (каждую сессию)
1. Бери **одну** задачу. В начале дай агенту этот файл (00) + текущий промпт.
   Скажи: «сделай только задачу N, потом остановись».
2. Перед кодом — пусть покажет план изменений (что и где правит).
3. Рефакторинги (03–06) — **behavior-preserving**: поведение не меняем, только
   раскладку/абстракцию. Перед каждым — git-коммит (точка отката).
4. Конец задачи = `dotnet build` без ошибок + чек-лист промпта + коммит.
5. Следующую задачу — желательно в новой сессии (чистый контекст).
6. Если агент без доступа к репозиторию — вместе с промптом вставляй реальные
   файлы, на которые он ссылается (иначе выдумает API).

## Соглашения проекта (повторять агенту)
- Переиспользуем существующие типы; новые — только где их нет.
- Каждый компонент: `.razor` + `.razor.cs` (логика не в разметке).
- Диалоги — через `IDialogService` + `IMudDialogInstance` (как `ClayColumnFilterDialog`),
  не через `@bind-Visible`.
- Значения фильтра в SQL — только Dapper-параметрами; имя колонки — только из реестра колонок.
- partial-классы повторяют точную сигнатуру дженерика; база/интерфейсы — в одном файле.
