# QS1 — Разведка (ничего не менять)

> Предварительно прочитать `QS0_README_quick_search.md`.
> **На этом шаге запрещено менять любой файл.** Только чтение и письменный отчёт.

## Цель

Заменить догадки фактами. Дальше шаги ссылаются на имена, которых я не знаю заранее, — их надо установить здесь.

## Что прочитать

- `src/Clayzor.Lib.Web.Controls/AGENTS.md` и `src/Clayzor.Lib.Web.Controls/docs/clay-grid.md`
- `src/Clayzor.Lib.Web.Controls/Components/Grid/promts/_done/_readme_grid_dynamic.md` (план G0–G14)
- `src/Clayzor.Lib.DALC/AGENTS.md` (SQL-конвенции, пагинация 2008 R2)
- `src/Clayzor.Lib.Entities/DynamicGrid/` — `ClayGridDefinitionData`, `DynamicSql`, `ClayGridSchemaMap`
- `src/Clayzor.Lib.Web.Controls/Components/Grid/ClayGrid*.razor`, `ClayGrid.Dynamic.cs`
- `ClayColumnSettingsDialog` + `wwwroot/js/clayColumnSettings.js`
- `ColumnNames.cs`
- `scripts/dynamic-grid/schema.sql`

## Вопросы, на которые нужен письменный ответ

**А. Существующий поиск**
1. Как устроен параметр `SearchColumns` у `<ClayGrid>`: тип, формат, где превращается в SQL? Имя класса/метода SQL-билдера поиска.
2. Есть ли **уже** поле ввода поиска на панели управления грида? Как называется поле состояния, где обработчик, есть ли debounce?
3. Как поиск комбинируется с фильтрами и группировкой в итоговом SQL (через `AND`? оборачивается ли в скобки?).
4. Какие тесты в `tests/Clayzor.Lib.Web.Controls.Tests` покрывают поиск — имена файлов/тестов.

**Б. Динамический режим**
5. Каким SQL читаются определения колонок из `ClayGridColumns`: **явный перечень колонок или `SELECT *`**? Приведи текст константы и её место.
6. Как называется модель метаданных одной колонки в динамическом режиме (куда добавлять новое свойство) и где она заполняется из строки результата?
7. Как определяется таблица колонок — имя захардкожено или приходит параметром (`settingsTable` и т.п.)? Схема (`dbo`) фиксирована?
8. Как исходный `ClayGridSettings.[SQL]` оборачивается для фильтрации/пагинации (алиас `_src`?). Приведи пример готового SQL.

**В. Типы колонок**
9. Найди перечисление/справочник кодов `Тип int`. Полный список кодов с расшифровкой и место определения (enum? константы?).
10. Для каждого кода: значение колонки берётся **напрямую из исходного запроса** или подменяется/вычисляется на клиенте (справочник по `Формат`-SELECT, логическое по выражению, ссылка, вычисляемое)?

**Г. Пользовательские параметры**
11. Какие параметры **уже** сохраняются в `ClayGridUserParams`? Приведи фактические значения `Параметр` (полностью) и формат `Значение`.
12. Есть ли уже готовые методы загрузки/сохранения пользовательских параметров? Имена и подписи. Как формируется `Параметр` — включает ли он `КодЗапроса`?
13. Чем определяется `КодНастройкиКлиента` — где берётся идентификатор текущего пользователя?
14. Есть ли уже константы-префиксы параметров? Как они названы и какова длина их значений?

**Д. Диалог настройки колонок**
15. Какие параметры есть у `ClayColumnSettingsDialog` (в т.ч. `ShowSorting`), откуда он вызывается (тулбар `ViewColumn`, prompt печати/экспорта) и как результат применяется к гриду?
16. Как реализованы существующие переключатели в диалоге (видимость колонки) — через `ClayCheckbox`? Как передаётся результат в C# (`OnJsDrop`)?
17. Что делают кнопки `ClearAll` и `RestartAlt` — что именно они восстанавливают?

## → verify

Отчёт на 15–25 строк: по одному короткому пункту на каждый вопрос, **с указанием файла и строки**. Отдельно в конце:

- **Список кодов `Тип`, которые я считаю допустимыми для быстрого поиска, и почему** (предварительно, окончательно — в QS4).
- **Фактическое значение `Параметр` для существующих пользовательских настроек** — от него зависит формат ключа в QS5.
- Список мест, где я не нашёл ответа и вынужден задать вопрос заказчику.

Если какой-то ответ не найден в коде — **не придумывай**. Отметь как «не найдено» и задай вопрос через question tool.
